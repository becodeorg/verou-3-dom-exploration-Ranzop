const getPar = document.getElementsByTagName ("p");

console.log(getPar[2].innerHTML);

const areParClass = document.getPar.classList.contains(class);
for (let i of areParClass) {
    console.log (i);
}

/* if areParClass === TRUE {
    console.log (reParClass
}
 */